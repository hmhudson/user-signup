def getRandomMovie(self):

    return "The Big Lebowski"

def get(self):
    movie = self.GetRandomMovie()

    content= <h1>"Movie of the Day"</h1>
    content = <h2> + movie + </h2>


error = self.request.get(username_error)
multiple error parameters
function to verify username etc, then pass it through class

class Handler():
    def post(self)

def param_test(first, *args, **kwargs):
    print('first', first)

    for key in kwargs.keys():
        print(key + ';' + kwargs[key])

    for i in range(len(args)):
        print('args')[' + str(i) + '], arg
